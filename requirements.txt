# Local (data prep) — no GPU needed, no extra installs required beyond
# the Python standard library. Kept here for completeness/documentation.

# Training + inference (run inside Google Colab with a GPU runtime):
unsloth
trl
peft
accelerate
bitsandbytes
transformers
datasets
torch
huggingface_hub
